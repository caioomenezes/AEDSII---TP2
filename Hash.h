//Amanda(5366), Caio(5784), Leticia(5781), Melissa(5384)


#include "ListaIngredientes.h"

//verificar includes
#include <sys/time.h>
#include <stdlib.h>
#include <string.h>
#define VAZIO  "!!!!!!!!!!"
#define RETIRADO  "**********" 
#define M 17 //Tamanho da tabela Hash   
#define TAMALFABETO 256

/*ta no lista
typedef char TipoChave[N];
typedef unsigned TipoPesos[N][TAMALFABETO];
typedef struct TipoItem {
  TipoChave Chave;
} TipoItem;

typedef unsigned int TipoIndice;
typedef struct TipoCelula* TipoApontador;

typedef struct TipoCelula {
  TipoItem Item;
  TipoApontador Prox;
} TipoCelula;

typedef struct Lista_Encadeada_Ingrediente{ //
  TipoCelula *Primeiro, *Ultimo;
} Lista_Encadeada_Ingrediente;

typedef Lista_Encadeada_Ingrediente Hash[M];

Hash Tabela;
TipoItem Elemento;
TipoPesos p;
TipoApontador i;


*/